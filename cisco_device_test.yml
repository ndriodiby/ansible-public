 - name: Protocol Configuration
   hosts: nexus9300_01
   connection: network_cli
   gather_facts: no

  

   tasks:

    
     - name: show configuration
       nxos_config:
         src: "nxos.txt"
